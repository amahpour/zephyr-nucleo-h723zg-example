# SPDX-License-Identifier: Apache-2.0
# Board-specific configuration for QEMU x86 (simulator)

# Select simulator target
CONFIG_APP_TARGET_SIM=y

# ADC subsystem
CONFIG_ADC=y

# ADC emulator driver
CONFIG_ADC_EMUL=y

# Number of ADC channels (matches hardware configuration)
CONFIG_APP_NUM_CH=15


# Integration test dependencies
pytest>=7.0
pyserial>=3.5
pyyaml>=6.0

# No instrument libraries: every current rig drives its stimulus through the
# DUT's own shell (adcset on QEMU, dacset on the virtual PCB and on hardware).
# The Rigol DP832 and KB2040 mux packages left with the rig that used them.
